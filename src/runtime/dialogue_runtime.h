#ifndef YAAT_RUNTIME_DIALOGUE_RUNTIME_H
#define YAAT_RUNTIME_DIALOGUE_RUNTIME_H
/* Dialogue runtime state and helpers are compiled into main_win32.c so they can
 * coordinate with script dispatch and Win32 input without owning those systems. */
#endif
