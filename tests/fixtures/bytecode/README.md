# Bytecode fixtures

Generated `.yaatbc` files are intentionally not committed because they are binary artifacts.
Run `make fixtures` from the repository root to regenerate bytecode samples from the source fixtures under `tests/fixtures/scripts/`.
