# game/scripts/startup.yaat
# Startup state for the text-only demo asset tree.

var door_locked = true
var intro_seen = false
var took_key = false
var demo_complete = false

room startup_state {
  on enter {
    say narrator "YAAT text demo state is ready."
  }
}
