# game/scripts/global.yaat
# Shared placeholder interactions kept as data, not engine code.

hotspot missing_asset_note {
  name "Missing Asset Note"

  on look {
    say narrator "Placeholder BMP and WAV paths are present for validator coverage."  # @scripts.global.1
  }
}
